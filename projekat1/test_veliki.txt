# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
# Kompresija i zaštita podataka — projektni zadaci

Dva nezavisna projekta, isti repo. Java, bez build alata ako nije neophodno (`javac`/`java` je dovoljno).

## Struktura

Dva samostalna foldera, bez zajedničkog koda. Svaki se kompajlira i predaje nezavisno.

```
projekat1/
  src/kompresija/   Main.java + algoritmi
  izvestaj.txt
  (testni fajlovi stoje tu u projekat1/, bez posebnog foldera)
projekat1.pdf, projekat2.pdf   tekstovi zadataka

Projekat 2 (LDPC) se NE radi zasad — folder ne postoji, pravi se tek kad se krene.
Sekcija o njemu je niže u ovom fajlu i važi kad dođe red.
```

```bash
javac -d projekat1/out projekat1/src/kompresija/*.java
java -cp projekat1/out kompresija.Main
```

---

## ⛔ Tvrda ograničenja (iz teksta zadatka)

**Zabranjeno je korišćenje nestandardnih biblioteka, naročito onih koje uključuju delimičnu ili potpunu implementaciju traženih metoda.**

Konkretno to znači:

- Samo **JDK standardna biblioteka**. Bez Guava, Apache Commons, bilo čega sa Mavena.
- **`java.util.zip` je zabranjen u celini** — `Deflater`, `Inflater`, `GZIPOutputStream`, `CRC32`. To je standardna biblioteka, ali sadrži gotovu implementaciju LZ77+Huffman.
- Bez `java.util.Base64` za pakovanje podataka.
- Dozvoljeno: `java.io`, `java.nio.file`, `java.util` (kolekcije, `Random`, `BitSet`), `java.util.stream`, `Math`.

Ako ti se čini da bi neka klasa iz JDK-a rešila pola zadatka — verovatno je zabranjena. Pitaj pre nego što je upotrebiš.

---

## Konvencije (važe za oba projekta)

- **`byte` je signed.** Svaki put kad se bajt koristi kao broj: `b & 0xFF`.
- **Bit-order: MSB-first**, isto u writeru i readeru. Nikad ne mešaj.
- **`>>>` a ne `>>`** pri radu sa bitovima.
- **`long`** za veličine fajlova, brojače i `N` u headeru.
- **Nema `String` nad binarnim podacima.** Ni kao ključ mape, ni kao međureprezentacija — `new String(bytes)` kvari bajtove ≥ 0x80. Koristi `byte[]` ili celobrojne ključeve.
- `Math.log` je prirodni logaritam → `/ Math.log(2)`.
- **Bez komentara u kodu.** Kod mora da bude razumljiv sam po sebi; imena promenljivih na
  engleskom. Ako je nešto kontraintuitivno, objasni to u chatu, ne u fajlu.
- **Bez CLI-ja uopšte.** Jedan `main` koji odradi sve i sve ispiše, bez argumenata.
  Ulazni fajl je konstanta `INPUT_FILE` na vrhu `Main` — menja se direktno u kodu.
- Bez preteranog apstrahovanja — ovo je projekat koji se brani usmeno, kod mora da bude čitljiv i objašnjiv u 2 minuta po klasi.

---

# PROJEKAT 1 — kompresija

## Zadatak

1. Bajt-entropija binarnog fajla: `H = −Σ pᵢ log₂ pᵢ`, `pᵢ = Nᵢ/N`, uz `0 log₂ 0 = 0`.
2. Shannon-Fano i Huffmanov kod nad `p₀…p₂₅₅`; u kodirani fajl prvo kod, pa podaci.
3. LZ77 i LZW nad alfabetom `A = {0,…,255}`.
4. Dekoder za svaki metod.
5. Test fajl 1–10 MB, stepen kompresije za svaki metod, `izvestaj.txt`.

## Format kodiranog fajla

Bodovano: *„osmisliti strukturu kodiranog fajla tako da se postiže (asimptotski) optimalna veličina"*. Bit-level pakovanje je obavezno, ne opciono.

```
HEADER (zajedničko, 11 B):
  magic     2 B    "KZ"
  metod     1 B    0=SF 1=HUF 2=LZ77 3=LZW
  N         8 B    dužina originala (long, big-endian)

SF / HUFFMAN:
  + 256 × 6 bita   dužine kodnih reči (KANONSKI), = 192 B

LZ77:
  + log2W   1 B
  + maxLen  1 B
  + minMatch 1 B

LZW:
  + startWidth 1 B
  + maxWidth   1 B
  + resetFlag  1 B

PODACI: čist bitski tok, padding nulama do granice bajta.
```

**Kanonski Huffman** — pamte se samo dužine, kod se rekonstruiše deterministički:

```java
int code = 0;
for (int len = 1; len <= maxLen; len++) {
    for (int sym = 0; sym < 256; sym++)
        if (length[sym] == len) codes[sym] = code++;
    code <<= 1;
}
```

6 bita po dužini je dovoljno: minimalna veličina fajla za Huffmanovu dubinu `d` je `F(d+2)`, a `F(35) ≈ 9.2M`, pa za ≤10 MB dubina ne prelazi ~34.

**Kraj podataka:** iz `N` u headeru — dekoduj tačno `N` bajtova i stani.

## Pakovanje LZ77 / LZW

LZ77: `[1 bit flag]`; flag=0 → `[8 bita literal]`; flag=1 → `[⌈log₂W⌉ bita: d−1][⌈log₂maxLen⌉ bita: k−minMatch]`.
Oduzimanje `−1` / `−minMatch` koristi pun opseg polja jer `d ≥ 1`, `k ≥ minMatch`.

LZW: fiksnih 12 bita, ili promenljiva širina 9→16.

## ⚠️ Rubni slučajevi koji MORAJU da rade

| Slučaj | Šta puca |
|---|---|
| Fajl sa 1 različitim bajtom | Huffman: koren je list, kodna reč dužine 0 → beskonačna petlja u dekoderu. Ručno dodeli dužinu 1. |
| Prazan fajl (0 B) | svuda |
| LZ77 preklapanje `d < k` | `System.arraycopy` daje pogrešan rezultat. **Kopiraj bajt po bajt unapred.** `(1,1,50)` mora dati 50 istih bajtova. |
| LZW **KwKwK** | Dekoder dobije indeks `== next` (reč koja se tek gradi). Rešenje: `entry = prev + prev.firstByte()`. Test: ulaz `ABABABA` (alfabet {A,B}) → kod `1,2,3,5`; kod prijema `3` sledeći je `5` koji ne postoji. |
| LZW prelazak širine indeksa | Koder i dekoder moraju povećati širinu u istom koraku; dekoder kasni jedan ulaz. Test na fajlu > 512 ulaza. |
| Slučajni / već `.zip` fajl | Svi metodi povećavaju veličinu. To je očekivano, ne bug. |
| `close()` na BitWriter | Bez flush-a se gubi do 7 bita. `try-with-resources`. |

## Verifikacija

Za svaki od 4 metoda: `encode → decode → diff` sa originalom mora biti prazan.

JUnit i svaki drugi test framework su **zabranjeni** (nestandardna biblioteka). Verifikacija
je deo onoga što `Main` ionako radi pri pokretanju: kodiraj, dekodiraj, uporedi bajt po bajt.
Sanity check: Huffman **ne sme** biti manji od `N·H/8` (nemoguće). LZ77/LZW **smeju** — oni koriste zavisnost među bajtovima, koju bajt-entropija ne meri.

## izvestaj.txt

Tabela: metod | veličina | header | bita/bajt | ušteda | vreme. Plus `N`, `H`, `N·H/8`, izabrani parametri sa obrazloženjem, i uputstvo za pokretanje (konkretne komande).

---

# PROJEKAT 2 — LDPC

## Parametri

`n = 15`, `n − k = 9`, `w_r = 5`, `w_c = 3`, `seed = broj indeksa`, `th₀ = th₁ = 0.5`.

## Zadatak

1. Konstrukcija `H` (Gallagerov metod).
2. Tabela sindrom–korektor + kodno rastojanje `d(C)`.
3. Gallager B algoritam.
4. Najlakše `e` koje Gallager B ne ispravlja; uporediti sa `d(C)`.

## Konstrukcija H

`(n−k)/w_c = 3` reda po grupi. Prva grupa blok-dijagonalna: red 0 → kolone 0–4, red 1 → 5–9, red 2 → 10–14. Preostale `w_c − 1 = 2` grupe su **permutacije kolona** prve grupe.

```java
Random rnd = new Random(brojIndeksa);      // FIKSIRAN SEED, obavezno
Collections.shuffle(perm, rnd);            // sa prosleđenim generatorom!
```

`Collections.shuffle(perm)` bez generatora uništava reproducibilnost — tiha i skupa greška.

**Provera odmah posle konstrukcije:** svaka kolona tačno 3 jedinice, svaki red tačno 5.

## Reprezentacija

`n = 15 ≤ 64`, pa svaki red `H` je jedan `long` bitmaska. Kodne reči i vektori greške su `int` bitmaske.

```java
int dot(long a, long b)      { return Long.bitCount(a & b) & 1; }   // skalarni proizvod nad GF(2)
int syndrome(long[] H, int x){ int s=0; for (int i=0;i<H.length;i++) s |= dot(H[i],x)<<i; return s; }
```

Sabiranje redova = `H[i] ^= H[j]`. Gaussova eliminacija nad GF(2) daje `rank(H)`.

## ⚠️ Očekivani rezultati (proveri na svom seed-u, ne prepisuj)

- **`rank(H) = 7`, ne 9.** Unutar svake grupe tri reda disjunktno pokrivaju svih 15 kolona → suma svake grupe je vektor svih jedinica → dve nezavisne zavisnosti. Dakle `k = 8`, kod ima **256** kodnih reči (ne 64), i **128** dostižnih sindroma (ne 512).
- **`d(C)` je verovatno 2**, jer `H` obično ima duple kolone: svaka kolona je određena trojkom (red u grupi 1,2,3) ∈ {1,2,3}³ → 27 obrazaca za 15 kolona. To nije bug.
- **Tačka 4: `w_fail = 1`.** Razlog su 4-ciklusi: kad je pogrešan samo `x_j`, on biva ispravljen (sva tri check čvora šalju 0), ali svaki `x_l` koji deli **dva** check čvora sa `x_j` dobija 2 od 3 poruke „postani 1" i prevrne se → nastaje nova greška.

## Gallager B

```
x⁽⁰⁾ = y
ponavljaj:
  ako je Hx⁽ᵏ⁾ᵀ = 0 → gotovo
  ω_i = Σ_{l ∈ N(c_i)} x_l                    (po check čvoru)
  ω_{i→j} = ω_i ⊕ x_j                          (poruka ka x_j, ISKLJUČUJE x_j)
  d_{j,0}, d_{j,1} = broj primljenih 0 / 1
  x_j = 0 ako d_{j,0} ≥ th₀·n_j
        1 ako d_{j,1} ≥ th₁·n_j
        y_j inače
```

Detalji koji se lako previde:

- **Svi čvorovi se ažuriraju istovremeno** (u novi niz), ne u mestu.
- **`y` se čuva netaknuto** kroz sve iteracije — grana „inače" vraća originalnu vrednost iz kanala.
- Provera zaustavljanja **pre** ažuriranja.
- `maxIter = 50`. Bez ograničenja dekoder može da osciluje zauvek.
- `n_j = w_c = 3`, `th·n_j = 1.5` → treba **≥ 2 od 3**. Grana `y_j` je mrtav kod (neparan stepen), ali je ostavi radi opštosti.

## Tačka 4 — pretraga

Pretpostavi poslatu **nultu** kodnu reč (sme se: `s = Hyᵀ = Heᵀ`, sindrom zavisi samo od greške) → ušteda faktora 256. Obilazi `e` po **rastućoj težini**, stani na prvom neuspehu.

**„Ne uspeva" ima dva slučaja:** dekoder ne konvergira (`Hrᵀ ≠ 0`), **ili** konvergira na pogrešnu kodnu reč (`Hrᵀ = 0` ali `r ≠ 0`). Provera mora biti `r != null && r == 0`, ne samo `r != null`.

## Ispis za izveštaj

`H` (9×15), `rank(H)`, `k`, broj kodnih reči, tabela sindrom–korektor, `d(C)` sa objašnjenjem koje su kolone zavisne, broj 4-ciklusa / girth, `w_fail` sa konkretnim `e`, i **trag prve iteracije** (koji su se bitovi prevrnuli).

---

## Kako radimo

- **Claude piše sav kod**, uključujući jezgra algoritama. Ne deli se posao, ne ostavljaju se
  TODO mesta „za Luku". Napiši celu stvar odmah i pokreni je.
- Objašnjenje ide **posle** koda, u chatu, jer se projekat brani usmeno.
- Posle svake implementirane metode: pokreni verifikaciju (`encode→decode→diff`) pre nego što pređemo dalje.
- Ako nešto ne razumem u kodu koji predložiš, objasni pre nego što nastaviš.
